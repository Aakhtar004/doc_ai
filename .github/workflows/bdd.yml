name: BDD Tests

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  analysis:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '18', cache: 'npm' }

      - name: Run BDD suite
        run: |
          npm install --save-dev @cucumber/cucumber
          mkdir -p docs/reports/bdd
          npx cucumber-js --format html:docs/reports/bdd/index.html || true

      - name: Sync with remote main (rebase)
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          git pull --rebase origin main || true

      - name: Commit BDD report
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          branch: main
          commit_message: "ci: update BDD report"
          file_pattern: docs/reports/bdd/**
          push_options: --force